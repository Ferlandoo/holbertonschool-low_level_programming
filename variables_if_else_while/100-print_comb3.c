#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a, b;

	for (a = 0; a <= 9; a++)
	{
		for (b = a + 1; b <= 9; b++)
		{
			putchar(i + '0');
			putchar(j + '0');
			if (i < 8 || j < 9)
				putchar(',');
			putchar(' ');
		}
	}
}
putchar('\n');
return (0);
}
