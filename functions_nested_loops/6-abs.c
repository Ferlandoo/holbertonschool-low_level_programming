#include "main.h"

/**
 * _abs - function computes the absolute value of the integer argument j
 * @n: the character to check
 * Return: 1 n is greater or equal zero,  -1 if n is less then zero
 */
int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	return (-n);
}
